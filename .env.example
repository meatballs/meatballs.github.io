# Swarm deployment configuration
# Copy this file to .env and fill in your actual values

# Batch ID from your bee node (required for deployment)
# Create with: swarm-cli postage stamp create --depth 20 --amount 0.1
SWARM_BATCH_ID=

# Feed manifest (automatically generated after first deployment)
# Leave empty initially - will be populated after first upload
SWARM_FEED_MANIFEST=

# Bee node endpoints (usually these defaults work for local bee node)
BEE_API_URL=http://localhost:1633
BEE_DEBUG_API_URL=http://localhost:1635